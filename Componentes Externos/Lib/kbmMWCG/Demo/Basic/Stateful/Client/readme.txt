kbmMW stateful client demo
---------------------------

This project contains a demo client which is using a traditional
request/response transport to communicate with an application server.

The demo needs the stateful demo application server to run.

It shows how to make simple stateful client to server calls.

