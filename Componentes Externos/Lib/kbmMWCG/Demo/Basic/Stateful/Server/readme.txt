kbmMW stateful server demo
---------------------------

This project contains a demo server is using a traditional
request/response transport to allow clients to access it.

The demo provides a stateful service along with an inventory service.
The client can access the stateful service owned by the
client and get an incremented number for each call.



